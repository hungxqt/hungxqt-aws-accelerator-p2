# Scenario Guide

Use `examples/minimal` for a validation-oriented starting point and `examples/complete` when you want a fuller root-module shape with common naming/tagging inputs.

## Upstream Scenario Catalog

- `complete`
- `session-manager`

Project-specific scenario values belong in consuming roots. This wrapper should only change when the shared upstream module contract changes.
